package krys.paladin;

/** Rodzaj wartości źródłowej przypisanej do modyfikatora ulepszenia. */
public enum UpgradeDamageValueKind {
    PERCENT_X,
    PERCENT_PLUS,
    COMPONENT_PERCENT,
    HIT_COUNT,
    SECONDS,
    TEXT_ONLY,
    NONE,
    NEEDS_MANUAL_REVIEW
}
